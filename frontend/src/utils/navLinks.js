const navLinks = [
    { name: "Chat", path: "/", isReact: true }, // React route
    { name: "Profile", path: "http://localhost:8000/users/", isReact: false }, // Django rendered
];

export default navLinks;